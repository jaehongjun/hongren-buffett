import { GET_MARKET_ALL_TYPES } from "./actions";

const initialState = {
  detect: [],
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
